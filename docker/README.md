# Docker Images

Dockerfiles for production builds.

## Files

- `Dockerfile.api` - API backend (multi-stage build)
- `Dockerfile.worker` - Worker (multi-stage build)
- `Dockerfile.web` - Frontend (Nginx + static build)

## Coming in Phase 11
