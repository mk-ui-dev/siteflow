# Infrastructure Scripts

Deployment and maintenance scripts.

## Scripts

- `install_vps.sh` - Ubuntu VPS setup (Docker, firewall, fail2ban)
- `backup.sh` - Database and MinIO backup
- `restore.sh` - Restore from backup
- `smoke.ts` - Smoke tests for production

## Coming in Phase 11
